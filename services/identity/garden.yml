kind: Build
name: identity
type: container

# Identity deploys in all environments (registry for member KEL, nodes for gossip, standalone for mail)

source:
  path: ../..  # Set build context to repository root

include:
  - lib/kels/**/*
  - lib/derive/**/*
  - lib/mock-hsm/**/*
  - services/identity/**/*
  - project.garden.yml

spec:
  dockerfile: services/identity/Dockerfile

---

kind: Deploy
name: identity
type: kubernetes

# Identity deploys in all environments (registry for member KEL, nodes for gossip, standalone for mail)

dependencies:
  - build.identity
  - deploy.postgres

variables:
  identityDatabaseUrl: ${var.postgres.url}/identity
  identityKeyHandlePrefix: "${var.envType == 'registry' ? 'registry' : 'gossip'}"
  kelForwardUrl: "${var.envType == 'registry' ? 'http://registry:' + var.registry.port : var.kels.url}"
  kelForwardPathPrefix: "${var.envType == 'registry' ? '/api/v1/member-kels' : '/api/v1/kels'}"

spec:
  manifestTemplates:
    - manifests.yml.tpl
