# FileSadStore

**Canonical name:** `FileSadStore`

A filesystem-backed [`SadStore`](sad-store.md) implementation, used for offline / archival flows (export, backup, CLI development).

**Consumers:** CLI; offline tooling; tests.

**Dependencies:** [`sad-store.md`](sad-store.md).

Stub. Detailed content lands iteratively.
